# libaries are essentially large collections of code that others have written for us
import math # has lots of useful math functions

import random # this is a pseudo random number generator library

import re # this library implements regular expressions

import os # this lets you interact with your operating system

import json 

import nltk # the natural language toolkit

import matplotlib # data viz library

random_integer = random.randint(0,100)

# we can alias libraries with the as keyword
import pandas as pd
import seaborn as sns
from pandas import DataFrame
from pandas import *

# import specfic things from a library
# sometimes we only need a single module
from matplotlib import pyplot
from matplotlib import pyplot as plt
import matplotlib.pyplot as plt

import my_functions

df = pd.DataFrame()
df = DataFrame()